# v168 記録ボタン動作修正版

メニューの「記録」タブ、地図上の「記録ピン追加」、現在地メモ登録から記録パネルが開くように修正しました。

- 地図と初期ポイントは維持
- 既存の釣果記録は削除しない
- 記録を保存すると `mie-bass-catches-v1` に保存し、記録一覧と釣果ピンを更新
- `index.html` は `v168-record-button-fix` に置換してください
